---
title: "Lucha Libre Restaurant - Development Roadmap"
updated: "2025-10-14"
status: "Phase 2 in progress — persistence & UX implemented"
---

### Lucha Libre Restaurant - Development Roadmap

#### Phase 1: Critical Fixes 🔧 (COMPLETE ✅)

- Menu & Ordering System
  - [x] Fix menu display system (items not showing)
  - [x] Implement working category filtering
  - [x] Debug JavaScript rendering logic
  - [x] Test menu-items.json data loading
  - [x] Verify menu-categories.json loads correctly

- State Management
  - [x] Remove all localStorage usage from app.js
  - [x] Convert to in-memory state (variables/objects)
  - [x] Test state persistence during session
  - [x] Document new state management approach

- Homepage Content
  - [x] Remove repetitive paragraphs (reduce 3x repeated content to 1x)
  - [x] Diversify wrestling/masked wrestler metaphors
  - [x] Keep unique branding, reduce redundancy
  - [x] Review overall messaging clarity

- Accessibility & SEO
  - [x] Add descriptive alt text to all images
  - [x] Add alt text to food item images
  - [x] Add alt text to logo and decorative elements
  - [x] Verify semantic HTML structure
  - [x] Check image file paths and formats

---

#### Phase 2: Functionality 🚀 (PRIORITY — IN PROGRESS)

- Cart System
  - [x] Implement working add-to-cart functionality (cart.js + cart-ui.js)
  - [x] Create cart display/modal (cart modal implemented in cart-ui.js)
  - [x] Add quantity adjustment controls (plus/minus in menu + modal qty controls)
  - [x] Implement remove from cart option (remove control in modal)
  - [x] Display running cart total (subtotal shown in modal and badge updated)
  - [x] Implement session persistence (Option B) — cart persists in-session via sessionStorage under key `lucha_cart_v1`
  - [x] Emit cart change events — window dispatches `cart:changed` with cart summary
  - [x] Replace blocking alerts with accessible, non-blocking toast notifications
  - [x] Add accessible aria-live announcer and focus-trapped modal
  - [~] Cross-tab sync: NOT enabled (sessionStorage is per-tab). Option to add localStorage toggle remains.

- Navigation & Links
  - [x] Add nav active-state helper (setActiveNavLink() in app.js)
  - [~] Fix navigation active states — helper added; site-wide styling polish still recommended
  - [ ] Clarify menu vs order navigation (separate pages)
  - [ ] Update commented-out menu references
  - [ ] Ensure all links are functional
  - [ ] Test navigation on all pages

- Interactive Elements
  - [x] Add hover/focus small polish for add-to-cart and cart actions (CSS)
  - [ ] Make "Leave Feedback" button open modal/form
  - [ ] Create feedback form (even if not functional yet)
  - [ ] Make delivery service images clickable
  - [ ] Link delivery images to actual services (UberEats, DoorDash, etc.)
  - [ ] Add more polished hover states and transitions

- Layout Issues
  - [ ] Fix footer overlap on content pages
  - [ ] Adjust spacing and padding consistency
  - [ ] Verify responsive design on mobile
  - [ ] Test on tablet/desktop viewports
  - [ ] Fix hero text sizing on mobile

---

#### Phase 3: Enhancements ✨

- Animations & Interactivity
  - [ ] Add smooth page transitions
  - [ ] Implement hover effects on menu items
  - [ ] Add loading states for cart actions
  - [ ] Create smooth category transitions
  - [ ] Add button press feedback

- Menu Filtering
  - [ ] Add vegetarian filter option
  - [ ] Add spicy level indicator
  - [ ] Add dietary restriction filters
  - [ ] Implement multiple filter combinations
  - [ ] Show filter results count

- Content Expansion
  - [ ] Create actual privacy policy page
  - [ ] Update "BlackRock Design Haus" link context
  - [ ] Add more detailed item descriptions
  - [ ] Include ingredient lists or allergen info
  - [ ] Add staff recommendations

- Mobile Optimization
  - [ ] Create mobile navigation menu (hamburger)
  - [ ] Optimize touch targets for mobile
  - [ ] Ensure text readability on small screens
  - [ ] Test performance on mobile devices
  - [ ] Optimize image sizes for different devices

- Social Proof & Trust
  - [ ] Add placeholder/mock customer reviews
  - [ ] Add star ratings display
  - [ ] Include location/hours information
  - [ ] Add phone number prominently
  - [ ] Create "Why Choose Us" section

---

#### Phase 4: Polish & Launch 🎉

- Testing
  - [ ] Test all functionality end-to-end
  - [ ] Cross-browser testing
  - [ ] Performance optimization
  - [ ] SEO audit and optimization
  - [ ] Accessibility audit (WCAG compliance)

- Deployment
  - [ ] Prepare production build
  - [ ] Set up hosting
  - [ ] Configure deployment pipeline
  - [ ] Test in production environment
  - [ ] Monitor for errors

- Post-Launch
  - [ ] Gather user feedback
  - [ ] Fix any bugs discovered
  - [ ] Plan next iteration features
  - [ ] Monitor analytics
  - [ ] Plan seasonal updates

---

#### Notes & Considerations (current)

- Files changed / implemented in this sprint:
  - cart.js — replaced with sessionStorage-persisted cart (exports singleton `cart`) and public createCart() factory.
  - cart-ui.js — replaced with toast-based, accessible modal UI listening to `cart:changed`.
  - app.js — updated with nav active-state helper and wiring so Add buttons include data attributes required by cart-ui.
  - styles.css — appended toast and small UX/collision fixes (bump animation, focus outlines).
- Key technical details:
  - Persistence: sessionStorage used (per-tab). Cart summary is saved under key `lucha_cart_v1`.
  - Eventing: `window` emits a `cart:changed` CustomEvent with the cart summary in detail so other modules can react.
  - Accessibility: aria-live announcer, focus trap in modal, aria-labels on controls.
  - Price handling: prices stored and handled as integer cents in cart.js to avoid floating-point issues.

- Decisions made (so you don't need to re-approve):
  - Option B chosen: sessionStorage persistence (per-tab). This improves UX on reload within same tab and avoids cross-tab sync complexity.
  - Non-blocking toast implemented to replace alert() placeholders.

- Quick risks / TODOs to address asap:
  - Ensure cart-ui.js and cart.js are loaded as ES modules on all pages that have Add buttons (index.html, order/index.html). Relative pathing matters — verify script tags.
  - QA link paths (menu JSON fetch paths are relative; confirm hosting structure).
  - Cross-tab behavior expected: carts do not sync between tabs when using sessionStorage. If cross-tab sync is required, switch to localStorage with optional conflict handling.

---

#### Recommended next steps (highest priority)

1. Cross-tab persistence decision:
   - Option A: Keep sessionStorage (current) — per-tab persistence, simpler.
   - Option B: Enable localStorage toggle (recommended if users may open multiple tabs and expect shared cart). I can implement a toggle constant in cart.js that defaults to sessionStorage but can be set to localStorage.

2. Script injection & patch:
   - I can produce a single .diff patch that replaces files (cart.js, cart-ui.js, app.js, styles.css) and updates index.html / order/index.html to include correct `<script type="module">` tags with correct relative paths. This is the fastest way to apply changes.

3. QA pass:
   - Run the verification checklist across desktop and mobile:
     - Add items → toast appears, badge updates
     - Open cart modal → items, qty controls, remove, clear, subtotal work
     - Refresh page → cart persists (sessionStorage)
     - Cross-tab behavior → expected no sync
     - Accessibility checks with a screen reader: aria-live announcements & keyboard focus trap
   - Report any layout overlaps (footer, hero text) to triage into Phase 2 layout fixes.

4. Polish UX:
   - Replace remaining alert() placeholders with accessible modals or toasts (checkout flow scaffold).
   - Add feedback form modal and wire delivery partner links.

---




